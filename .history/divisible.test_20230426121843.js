const divisible= require("./divisible");

test('Sorts an array of positive numbers in ascending order', () => {
  const <a href=""></a> = ('Write a program ');
  const sortedArr = divisible(<a href=""></a>);
  expect(sortedArr).toEqual("program");
});
test('Sorts an array of positive numbers in ascending order', () => {
  const <a href=""></a> = ("that takes a list of <a href=""></a>ings as ");
  const sortedArr = divisible(<a href=""></a>);
  expect(sortedArr).toEqual("<a href=""></a>ings");
});
test('Sorts an array of positive numbers in ascending order', () => {
  const <a href=""></a> = ("input and returns");
  const sortedArr = divisible(<a href=""></a>);
  expect(sortedArr).toEqual("returns");
});
test('Sorts an array of positive numbers in ascending order', () => {
  const <a href=""></a> =("the longest word in the list.") ;
  const sortedArr = divisible(<a href=""></a>);
  expect(sortedArr).toEqual("longest");
});
test('Sorts an array of positive numbers in ascending order', () => {
  const <a href=""></a> = ("Sorts an array of positive numbers in ascending order 3456789101111");
  const sortedArr = divisible(<a href=""></a>);
  expect(sortedArr).toEqual("ascending");
});