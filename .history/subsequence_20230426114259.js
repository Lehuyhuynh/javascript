function subsequence(a,b)
{
  var n = a.size();  // n chiều dài xâu a, m chiều dài xâu b
    var m = b.size();
    var max_Size;     // Biến lưu độ dài con chung lớn nhất
    let subsequence = ""; 
}
module.exports= subsequence