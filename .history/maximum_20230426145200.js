function maximum(a)
{
  sum=0
  for(var i=0;i++; i+3 <= a.length){
    sum=a[i]+a[i+1]+a[i+2]+a[i+3]
    if(sum1 < sum){
      return sum
    }
    else{return sum1}
  } 
  return sum;
}
maximum([2,4,5,7,10])

module.exports= divisible