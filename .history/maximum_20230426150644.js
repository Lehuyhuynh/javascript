function maximum(a[], n,k)
{
  if(k >=n) return 0
  var MaxSum = 0, Sum = 0;  // Max sum là tổng lớn nhất,Sum là tổng của k phần tử liên tiếp
  var count; // biến đếm tạm  để tí gán bằng k.
  var tempI; // biến đếm tạm để tí gán bằng i
}
maximum([2,4,5,7,10])

module.exports= divisible