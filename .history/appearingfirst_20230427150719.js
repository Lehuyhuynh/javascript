function  appearingfirst(array) {
  var num_array=array.sort()
  num_array.forEach(function (x,i) {
    let len=[] =0 ;
    if(len[] num_array[i].length){

    }
  });
  return array
}
appearingfirst(['apple', 'manana', 'crange', 'btermelon'])
module.exports= appearingfirst